{% extends "_layout" %}
{% set title = entry.title %}
{% block main %}

<section class="uk-section">
<div class="uk-container uk-container-medium">

	{{ entry.id }}
	{{ entry.title }}
  {{ entry.author }} on {{ entry.postDate|date('d M Y') }}.
	{{ entry.redactor }}

</div>
</section>

{% endblock %}
